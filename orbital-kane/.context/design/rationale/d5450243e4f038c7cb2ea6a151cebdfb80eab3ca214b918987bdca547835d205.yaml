anchor: ac-81
combinatorial:
  reason: Applied equivalence partitioning to separate the interactive disclosure path from passive dashboard review. Applied state/flow because the scenario changes from warning-visible to popover-open. No covering array was needed because the source defined a single interaction path rather than multiple independent inputs; risk-judged combinatorial strength remained 2 (no money, auth, or data-loss factors applied to the grounded behavior).
  strength: 2
deferred_bindings: 0
expected_how: 'expected_mode: authored'
profile_family: unspecified
surface_state: absent
technique: Applied equivalence partitioning to separate the interactive disclosure path from passive dashboard review. Applied state/flow because the scenario changes from warning-visible to popover-open. No covering array was needed because the source defined a single interaction path rather than multiple independent inputs; risk-judged combinatorial strength remained 2 (no money, auth, or data-loss factors applied to the grounded behavior).
