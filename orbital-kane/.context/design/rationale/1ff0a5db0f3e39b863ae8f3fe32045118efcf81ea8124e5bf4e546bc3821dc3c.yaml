anchor: ac-99
combinatorial:
  reason: Applied state / flow because the source promises a collapsed-to-expanded state change on click. Kept this as a separate happy path from the single-expansion boundary case because expanding one alert proves inline expansion and non-modal behavior without involving the second-alert transition. No combinatorial covering array was needed because the path has no independent variable set to combine.
  strength: 2
deferred_bindings: 0
expected_how: 'expected_mode: authored'
profile_family: unspecified
surface_state: absent
technique: Applied state / flow because the source promises a collapsed-to-expanded state change on click. Kept this as a separate happy path from the single-expansion boundary case because expanding one alert proves inline expansion and non-modal behavior without involving the second-alert transition. No combinatorial covering array was needed because the path has no independent variable set to combine.
