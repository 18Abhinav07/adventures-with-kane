anchor: ac-1
combinatorial:
  reason: Applied state / flow and negative reasoning to the source's forbidden routing promise across a multi-hop navigation sequence. Used representative section switching instead of all permutations because the outcome is the same single-page in-place update for each hop. Risk-gated covering strength was 2 because no money, auth, or data-loss factor applies; no covering array was needed for this single dominant sequence path.
  strength: 2
deferred_bindings: 0
expected_how: 'expected_mode: authored'
profile_family: unspecified
surface_state: absent
technique: Applied state / flow and negative reasoning to the source's forbidden routing promise across a multi-hop navigation sequence. Used representative section switching instead of all permutations because the outcome is the same single-page in-place update for each hop. Risk-gated covering strength was 2 because no money, auth, or data-loss factor applies; no covering array was needed for this single dominant sequence path.
